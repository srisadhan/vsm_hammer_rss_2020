from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Optimal control of a Variable stiffness mechanism for hammering task',
    author='sri sadhan',
    license='MIT',
)
